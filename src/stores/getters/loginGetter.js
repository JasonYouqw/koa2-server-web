export const unit = (state) => {
  return state.unit;
  // return window.localStorage.getItem('ec-unit');
};

export const flag = (state) => {
  return state.flag;
  // return Boolean(window.localStorage.getItem('ec-flag'));
};
